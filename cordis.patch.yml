# dsh-design-skills bundle: registers every skills/<name>/SKILL.md in this
# package on ctx.skills. Applied after @deepseek-ai/dsh-base, which already
# mounts the skill registry and the model-facing skill loader.
- insert:
    - id: dsh-design-skills
      name: dsh-design-skills
